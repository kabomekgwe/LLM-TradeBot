# Production Environment Configuration Template
# Copy this file to .env and fill in your actual credentials
# NEVER commit .env to git (protected by .gitignore)
#
# PRODUCTION DEPLOYMENT NOTES:
# 1. Set TRADING_TESTNET=false only when ready for real money trading
# 2. Use strong API keys with IP whitelisting enabled
# 3. Set appropriate position limits based on account size
# 4. Enable notifications for critical alerts
# 5. Keep .env file secure (chmod 600 .env)

# ============================================================
# EXCHANGE API CREDENTIALS
# ============================================================
# SECURITY: Use API keys with minimal necessary permissions
# Enable IP whitelisting on exchange if available
# For production: Use separate keys from development/testnet

# Binance Futures (perpetual contracts)
BINANCE_FUTURES_API_KEY=your_binance_futures_key_here
BINANCE_FUTURES_API_SECRET=your_binance_futures_secret_here

# Binance Spot (spot trading)
BINANCE_SPOT_API_KEY=your_binance_spot_key_here
BINANCE_SPOT_API_SECRET=your_binance_spot_secret_here

# Kraken
KRAKEN_API_KEY=your_kraken_key_here
KRAKEN_API_SECRET=your_kraken_secret_here

# Coinbase
COINBASE_API_KEY=your_coinbase_key_here
COINBASE_API_SECRET=your_coinbase_secret_here

# Alpaca (stocks/crypto)
ALPACA_API_KEY=your_alpaca_key_here
ALPACA_API_SECRET=your_alpaca_secret_here

# Paper Trading (simulated - no real credentials needed)
PAPER_API_KEY=test_key
PAPER_API_SECRET=test_secret

# ============================================================
# TRADING CONFIGURATION
# ============================================================

# Trading Symbol (can be overridden by CLI)
TRADING_SYMBOL=BTC/USDT

# Default provider (binance_futures, binance_spot, kraken, coinbase, alpaca, paper)
# PRODUCTION: Use real exchange provider
# DEVELOPMENT: Use paper trading
TRADING_PROVIDER=binance_futures

# Testnet mode (true = safe testing, false = REAL MONEY!)
# PRODUCTION: Set to false for real trading
# DEVELOPMENT: Set to true for safety
# WARNING: Setting this to false will trade with REAL MONEY!
TRADING_TESTNET=true

# Risk Management Parameters
# PRODUCTION: Adjust based on account size and risk tolerance
TRADING_MAX_POSITION_SIZE_USD=1000.0      # Maximum position size per trade
TRADING_MAX_DAILY_DRAWDOWN_PCT=5.0        # Daily loss limit (%)
TRADING_MAX_OPEN_POSITIONS=3              # Maximum concurrent positions

# Agent Configuration
TRADING_DECISION_THRESHOLD=0.6            # Minimum confidence (0.0-1.0)
TRADING_ENABLE_ML_PREDICTIONS=true        # Enable ML predictions
TRADING_ENABLE_ADVERSARIAL_VOTING=true    # Enable Bull vs Bear voting

# ============================================================
# NOTIFICATION SERVICES (Optional but recommended for production)
# ============================================================

# Master notification switch
# PRODUCTION: Set to true to receive critical alerts
NOTIFICATIONS_ENABLED=true

# Telegram Bot (Recommended for production)
# Get bot token from @BotFather on Telegram
# Get chat ID from @userinfobot
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
TELEGRAM_CHAT_ID=your_telegram_chat_id_here

# Discord Webhook (Optional)
# Get webhook URL from Discord Server Settings > Integrations
DISCORD_WEBHOOK=your_discord_webhook_url_here

# Email (SMTP) - Recommended for critical alerts
# For Gmail: Use App Password (not regular password)
# Enable 2FA and generate app password in account settings
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASSWORD=your_email_app_password_here
EMAIL_TO=recipient@example.com
EMAIL_FROM=your_email@gmail.com

# Slack Webhook (Optional)
# Get webhook URL from Slack App settings
SLACK_WEBHOOK=your_slack_webhook_url_here

# ============================================================
# WEB DASHBOARD
# ============================================================

# Dashboard enabled (true/false)
# PRODUCTION: Enable for monitoring
DASHBOARD_ENABLED=true

# Dashboard host
# DEVELOPMENT: 127.0.0.1 (localhost only)
# PRODUCTION: 0.0.0.0 (all interfaces for Docker)
# DOCKER: Must be 0.0.0.0 for container access
DASHBOARD_HOST=0.0.0.0

# Dashboard port
DASHBOARD_PORT=5173

# ============================================================
# SAFETY CONTROLS
# ============================================================

# Kill Switch Secret Key
# PRODUCTION: Generate strong random key (e.g., openssl rand -hex 32)
# Used for HMAC authentication on kill switch endpoints
KILL_SWITCH_SECRET=your_strong_random_secret_key_here

# ============================================================
# LOGGING
# ============================================================

# Log level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
# PRODUCTION: INFO or WARNING (less verbose)
# DEVELOPMENT: DEBUG (detailed logs)
LOG_LEVEL=INFO

# ============================================================
# DOCKER-SPECIFIC CONFIGURATION
# ============================================================
# These settings are optimized for Docker deployment
# No changes needed unless customizing container behavior

# Container timezone (optional)
TZ=UTC

# ============================================================
# DATABASE CONFIGURATION (Phase 12)
# ============================================================
# PostgreSQL + TimescaleDB for trade history persistence

# PostgreSQL password (PRODUCTION: Use strong random password)
POSTGRES_PASSWORD=your_secure_postgres_password_here

# Database connection URL
# Format: postgresql://user:password@host:port/database
# Docker: Use service name 'postgres' as host
# Local dev: Use 'localhost' as host
DATABASE_URL=postgresql://tradingbot:${POSTGRES_PASSWORD}@postgres:5432/tradingbot

# ============================================================
# PRODUCTION DEPLOYMENT CHECKLIST
# ============================================================
# Before deploying to production:
#
# [ ] 1. API keys are valid and have correct permissions
# [ ] 2. TRADING_TESTNET is set appropriately (false for real trading)
# [ ] 3. Position limits match account size and risk tolerance
# [ ] 4. KILL_SWITCH_SECRET is a strong random key
# [ ] 5. Notifications are enabled and tested
# [ ] 6. Dashboard is accessible and secure
# [ ] 7. .env file permissions are secure (chmod 600 .env)
# [ ] 8. .env file is NOT committed to git
# [ ] 9. Backup of .env file exists in secure location
# [ ] 10. All credentials are stored securely
#
# WARNING: Real money trading involves substantial risk of loss.
# Test thoroughly with testnet/paper trading first.
# ============================================================
